# CueCast 使用说明

CueCast 是一个 Chrome Manifest V3 扩展，用于录制和回放 Web UI 测试步骤。

## 安装

1. 打开 Chrome，进入 `chrome://extensions/`
2. 开启“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择扩展目录 `D:\King\sakura-playwright\cuecast`

## 本地验证环境

项目内置了本地 mock 服务和测试页面：

- 测试页面：`http://127.0.0.1:4173/test-lab/`
- 目标页面：`http://127.0.0.1:4173/test-lab/target.html`
- 本地 API：`http://127.0.0.1:4173/api`

启动方式：

```bash
node test-lab/mock-server.js --port 4173
```

## 基本使用

### 1. 打开测试实验室

在扩展弹窗中点击 `Open Local Lab`，进入测试实验室页面。

### 2. 查看用例

实验室默认会加载用例 `278`。页面会展示：

- 用例名称和描述
- 起始 URL
- 操作步骤列表
- 历史执行结果
- 桥接状态

### 3. 录制

点击“开始录制”后，扩展会：

- 打开或接管目标页面
- 注入 `content/bridge.js`
- 在目标页上注入录制脚本
- 捕获点击、输入、键盘等交互
- 将步骤发送到后台并保存到后端

### 4. 回放

点击“执行用例”后，扩展会：

- 从后端读取用例步骤
- 进入 CDP 或 DOM 回放路径
- 按步骤执行点击、输入、断言等动作
- 将执行结果回传到后端

## 基本原理

### 1. Background Service Worker

`background.js` 是扩展的控制中心，负责：

- 接收弹窗和页面消息
- 管理录制/回放状态
- 调用后端 API
- 使用 `chrome.debugger` 做 CDP 回放

### 2. Bridge 内容脚本

`content/bridge.js` 会在普通网页中常驻，用来：

- 接收页面通过 `window.postMessage` 发来的命令
- 把命令转发给后台
- 把后台消息再转回页面

### 3. 录制脚本

`content/recorder.js` 负责采集用户操作，包括：

- 点击
- 输入
- 键盘事件
- 选择器定位信息
- 页面截图与局部缩略图

### 4. 回放脚本

`content/player.js` 和 `modules/player-manager.js` 负责执行步骤。

其中：

- CDP 模式通过 `chrome.debugger` 模拟更接近真实浏览器行为
- DOM 模式是降级路径，直接通过页面 DOM 执行动作

## 文件结构

- `manifest.json`：扩展清单
- `background.js`：服务工作线程
- `modules/`：录制、回放、API 客户端
- `content/`：页面注入脚本
- `popup/`：扩展弹窗
- `../test-lab/`：本地实验室与 mock 数据

## 常见问题

### 1. 为什么一开始看不到扩展卡片

请确认已通过 `chrome://extensions/` 手动加载该目录。

### 2. 为什么默认接口指向本地地址

项目自带了本地 mock API，方便离线验证录制和回放。

### 3. 为什么有些步骤需要 CDP

像自然语言步骤、JSON 断言这类操作，DOM 方式信息不够完整，CDP 更稳定。

## 说明

这是一个可本地运行的复刻版项目，适合调试、验证和展示完整录制回放链路。
